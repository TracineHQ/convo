{
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v convo >/dev/null 2>&1 && { if command -v timeout >/dev/null 2>&1; then timeout 30s convo index >/dev/null 2>&1; else convo index >/dev/null 2>&1; fi; }; true"
          }
        ]
      }
    ]
  }
}
